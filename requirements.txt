# Core data libraries
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
pyarrow>=14.0.0  # parquet 영속 캐시 (.cache/ohlcv/{tf}/{ticker}.parquet)

# HTTP & web parsing
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Decision framework (Phase 2)
PyYAML>=6.0  # weights.yml 사용자 가중치 설정
hmmlearn>=0.3.0  # 시장 국면 HMM 분석
pydantic>=2.0

yfinance>=0.2.40
exchange-calendars>=4.0.0  # XKRX 거래일 캘린더 (core/dates.py trading_days_since)

# Testing
pytest>=7.4.0
